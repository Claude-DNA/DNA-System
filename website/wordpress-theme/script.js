/**
 * Claude DNA Theme Scripts
 */

// Add any custom JavaScript here
// For example: smooth scrolling, interactive elements, etc.

document.addEventListener('DOMContentLoaded', function() {
    // Theme is ready
    console.log('Claude DNA theme loaded');
});
